import Client from "@helium/http";

// const network = new Network({
//   baseURL: process.env.NEXT_PUBLIC_HELIUM_API_BASE_URL as string,
//   version: 1
// });
const client = new Client();

export default client;
